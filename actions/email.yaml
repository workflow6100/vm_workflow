---
  name: "mail"
  ref: angela_pack
  runner_type: "python-script"
  description: "Send an email."
  enabled: true
  entry_point: "email.py"
  parameters:
  email_to:
    type: string
    description: 'Group_Name of VM'
    required: true
